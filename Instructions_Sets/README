Here, you find two files.

One file is the instructions for the user(GRADIENT MIXER INSTRUCTION SET.pdf), and the other are the instrcutions for making the Gradient Mixer machine(Instructions set for building the Linear Gradient Mixer.pdf).
